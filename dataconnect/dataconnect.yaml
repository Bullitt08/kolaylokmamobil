specVersion: "v1beta"
serviceId: "kolaylokma"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kolaylokma-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
